import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stickman.escape',
  appName: 'StickmanEscape',
  webDir: 'dist/public'
};

export default config;
